# fragmentation
fragmentation.py -i input.smi -o frags.txt -c 32 -v

# convert fragments to standardized representation of core and context
frag_to_env_mp.py -i frags.txt -o r3.txt -r 3 -c 32 -v

#remove duplicated lines
sort -u

# count duplicated entries
sort r3.txt | uniq -c > r3_c.txt

# create DB and import csv file with unique lines to a table
import_env_to_db.py -i r3_c.txt -o fragments.db -r 3 -c -v
